import "./AppBar.css";

const AppBar = () => {
    return (
        <header className="header">
            <h2 className="header__logo">LessTube</h2>
        </header>
    );
};

export default AppBar;
